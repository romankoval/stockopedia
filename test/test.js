var assert = require('assert');
describe('Stockopedia', function() {
    describe('get dividend', function() {
        it('should return some number', function() {
            assert.equal([1, 2, 3].indexOf(4), -1);
        });
    });
});